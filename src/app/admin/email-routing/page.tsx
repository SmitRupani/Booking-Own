import Card from '@/components/ui/Card';

export default function AdminEmailRouting() {
  return (
    <div className="mx-auto max-w-7xl p-6">
      <h2 className="text-2xl font-semibold">Email Routing</h2>
      <p className="text-sm text-muted-foreground mt-2">Manage email templates and routing rules.</p>

      <div className="mt-6">
        <Card>Email routing placeholder</Card>
      </div>
    </div>
  );
}
