"use client";
import React from 'react';

// Placeholder AuthProvider — will integrate Clerk later per brief.
export function AuthProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

export default AuthProvider;
