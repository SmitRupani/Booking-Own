"use client";
import * as React from 'react'

export function Toaster() {
  // Placeholder: keep a DOM node for toast libraries to mount into
  return <div id="toaster-root" className="fixed bottom-4 right-4 z-50" />
}

export default Toaster
